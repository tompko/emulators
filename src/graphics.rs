pub struct Graphics {
}

impl Graphics {
    pub fn new() -> Graphics {
        return Graphics{}
    }

    pub fn draw(&mut self, x: usize, y: usize, sprite: Vec<u8>) -> u8 {
        return 0;
    }
}
